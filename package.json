{
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "build": "vite build",
    "serve": "vite preview",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d dist"
  },
  "type": "module",
  "dependencies": {
    "three": "^0.169.0",
    "vite": "^5.4.8"
  },
  "devDependencies": {
    "gh-pages": "^6.2.0",
    "vite-plugin-static-copy": "^2.0.0"
  }
}
